# Januscope Frontend Environment Variables

# API Configuration
VITE_API_BASE_URL=http://localhost:9876

# Application Configuration
VITE_APP_NAME=Januscope
VITE_COMPANY_NAME=Sky World Limited

# Environment
# Options: development, production, staging
NODE_ENV=development

# Encryption Configuration
# Enable end-to-end encryption for API requests/responses
VITE_ENCRYPTION_ENABLED=false

# Encryption key (base64-encoded AES-256 key)
# Generate with: openssl rand -base64 32
# Or use the generateEncryptionKey() function in encryption.config.ts
VITE_ENCRYPTION_KEY=

# Note: Keep this key secret and never commit it to version control
# In production, use environment variables or secure key management
